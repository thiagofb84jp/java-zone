package interfaces.shape;

public interface Shape {
    /**
     * Write a Java program to create an interface Shape with the getArea() method. Create three classes Rectangle,
     * Circle, and Triangle that implement the Shape interface. Implement the getArea() method for
     * each of the three classes.
     */
    double getArea();
}